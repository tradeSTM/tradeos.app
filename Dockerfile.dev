FROM node:18-alpine

WORKDIR /app

# Install dependencies first (caching)
COPY package*.json ./
RUN npm ci

# Copy rest of the application
COPY . .

# Build for development
RUN npm run build

EXPOSE 3003

CMD ["npm", "run", "dev"]
